When a task requires installing, discovering, or verifying a CLI tool, use the local skill at `skills/getcli/SKILL.md`.

Do not guess vendor-specific install commands first. Prefer `getcli` to search for the tool, install it, and verify it before proceeding with the main task.
